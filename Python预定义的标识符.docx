1、如何查询预定义的标识符

python有一个内置的函数dir()，可以返回对象属性列表，不带参数调用时，将返回python所有内置属性列表，其中__builtins__属性是存放所有python内置模块。

所以使用dir(__builtins__)可查询所有预定义的标识符。输出的结果中用大写字母引导的是异常名

2、Python对特殊的方法和变量使用特殊的命名方式-开头和结尾都用下划线，应尽量避免自定义名称使用该命名方式
