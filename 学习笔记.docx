1、	一共有四种逻辑操作符-身份操作符，比较操作符，成员操作符和逻辑运算符
2、	身份操作符is的用法：操作符两边的对象引用指向的对象是同一个对象则返回true，否则返回false。is not的用法则相反

3、	比较操作符有==（表示数值相等），<=（表示小于或等于），<（表示小于），>（表示大于），>=（表示大于或等于），!=（表示不等于）。仅对对象的数值进行比较

4、	身份操作符的is和比较操作符的==并不相同，前者是判断是否引用了同一对象，后者则是判断数值是否相等，如
a = “love”
b = “love”
如果使用a is b则返回false，因为两者用的不是同一个对象，而使用a == b则返回true

5、	python可以支持结链比较，如5<=a<=9，只要a取值在5和9之间即返回true。

6、	不同类型的数据不能比较，如字符不能和整数比较

7、	成员操作符in的用法：用于测试对象是否存在于字符串，元组或列表中，是则返回true，否则返回false。not in则相反

8、	逻辑运算符有三种：and, or, not。
and的用法：从左往右看，返回第一个false值，否则返回最后一个表达式
or的用法：从左往右看，返回第一个true值，否则返回最后一个表达式
not的用法：根据表达式的true或者false来反向判断
python中的false值包括NONE，False，所有值为0的数（整数，浮点，长整型，复数），“”（空字符串），[]（空列表），()（空元组），{}（空字典）。其他均是true值
