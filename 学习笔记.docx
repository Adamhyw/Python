1、	Python的对象区别于变量的一点是对象有相应的方法可以使用，而变量只能存储数值

2、	python不需要先声明对象，也不需要先指定对象的数据类型，直接使用对象=值即可创建对象并赋值

3、	对象名称必须以字母或下划线引导

4、	对象名称只能由非空格字符，数字和下划线组成

5、	对象名称没有长度限制

6、	Python识别大小写字母，如Last 和last并不是同一个对象

7、	任何对象都可以通过重新赋值修改数据类型，如
test=18
test=’wo’
第二次赋值则把test变为字符串
